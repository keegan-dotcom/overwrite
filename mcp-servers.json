{
  "mcpServers": {
    "overwrite": {
      "command": "python3",
      "args": ["-m", "agent.mcp_server"],
      "cwd": "${CLAUDE_PLUGIN_ROOT}",
      "env": {
        "PYTHONPATH": "${CLAUDE_PLUGIN_ROOT}"
      }
    }
  }
}
